import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

/** Topluluk akışı — değerlendirmeler, listeler, paylaşımlar (içerik sonraki fazda). */
export function CommunityScreen() {
  return (
    <ScreenPlaceholder
      icon="people-outline"
      title="Topluluk"
      subtitle="Değerlendirmeler, listeler ve keşifler burada akacak."
    />
  );
}

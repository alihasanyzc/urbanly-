import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

/** Profil — ilgi alanları, kaydedilenler, ayarlar (içerik sonraki fazda). */
export function ProfileScreen() {
  return (
    <ScreenPlaceholder
      icon="person-outline"
      title="Profil"
      subtitle="İlgi alanların, kaydettiklerin ve ayarların."
    />
  );
}

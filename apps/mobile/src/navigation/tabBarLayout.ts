/**
 * Yüzen cam tab bar'ın ölçü sabitleri — hem tab bar hem de ekranların alt boşluğu
 * tek kaynaktan okur (içerik bar'ın altında kalmasın).
 */
export const TAB_BAR_HEIGHT = 64;
/** Bar'ın ekran altından yüksekliği + nefes payı (ekranların alt paddingi bunu kullanır). */
export const TAB_BAR_SPACE = TAB_BAR_HEIGHT + 28;
